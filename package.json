{
  "bugs": {
    "url": "https://github.com/tslmy/md.js/issues"
  },
  "dependencies": {
    "three": "^0.144.0"
  },
  "description": "A toy molecular dynamics simulator in the browser.",
  "devDependencies": {
    "@eslint/js": "^9.34.0",
    "@types/node": "^24.3.0",
    "@types/three": "^0.179.0",
    "@vitest/coverage-v8": "^2.0.5",
    "c8": "^9.1.0",
    "eslint": "^9.34.0",
    "nodemon": "^3.1.10",
    "puppeteer": "^24.17.0",
    "serve": "^14.2.3",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.40.0",
    "vitest": "^2.0.5"
  },
  "homepage": "https://github.com/tslmy/md.js#readme",
  "license": "ISC",
  "name": "md.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tslmy/md.js.git"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf built/*",
    "cov:open": "open coverage/lcov-report/index.html || xdg-open coverage/lcov-report/index.html || true",
    "dev": "nodemon --watch src --ext ts,js --exec \"npm run build && npx serve -l 8000 .\"",
    "start": "npm run build && npx serve -l 8000 .",
    "test": "npm run build && vitest run",
    "test:cov": "npm run build && vitest run --coverage",
    "test:watch": "npm run build && vitest",
    "typecheck": "tsc --noEmit",
    "watch": "tsc --watch"
  },
  "type": "module",
  "version": "1.0.0"
}
